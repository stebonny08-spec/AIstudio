customtkinter>=5.2.2

# llama-cpp-python per eseguire modelli LLM locali in formato GGUF
# direttamente dentro l'applicazione (senza bisogno di Ollama o LM Studio)
llama-cpp-python>=0.2.90

# Ricerca web gratuita (il pacchetto si chiamava 'duckduckgo-search', è
# stato rinominato 'ddgs' dagli stessi sviluppatori)
ddgs>=9.0.0

# Parsing dei formati file
python-docx>=1.1.2
python-pptx>=0.6.23
openpyxl>=3.1.5
PyMuPDF>=1.24.0

# OCR (richiede anche il programma Tesseract installato sul sistema, vedi README)
pytesseract>=0.3.10
Pillow>=10.4.0

# RAG: embedding locali + indice vettoriale
sentence-transformers>=3.0.0
chromadb>=0.5.5

# Input vocale
SpeechRecognition>=3.10.4
PyAudio>=0.2.14

numpy>=1.26.4
requests>=2.31.0
