pywebview>=5.3

# llama-cpp-python per eseguire modelli LLM locali in formato GGUF
# direttamente dentro l'applicazione (senza bisogno di Ollama o LM Studio).
# Versione >= 0.3.0 richiesta per il supporto a chat_template_kwargs,
# usato per attivare/disattivare la thinking mode di MiniCPM5 tramite
# il parametro enable_thinking del chat template.
llama-cpp-python>=0.3.0

# Ricerca web (livello 3 del RAG), ristretta a un elenco di fonti
# affidabili predefinito (vedi core/trusted_sources.py). Il pacchetto si
# chiamava 'duckduckgo-search', è stato rinominato 'ddgs' dagli stessi
# sviluppatori.
ddgs>=9.0.0

# Parsing dei formati file
python-docx>=1.1.2
python-pptx>=0.6.23
openpyxl>=3.1.5
PyMuPDF>=1.24.0

# OCR locale con OpenCV e Tesseract (richiede anche il programma Tesseract
# installato sul sistema, vedi README)
opencv-python>=4.8.0
pytesseract>=0.3.10
Pillow>=10.4.0

# RAG: embedding locali + indice vettoriale.
# Nota: al primo utilizzo, sentence-transformers scarica automaticamente
# il modello 'intfloat/multilingual-e5-small' (~470MB) dalla cache di
# Hugging Face. Va fatto una volta sola, poi resta in locale.
sentence-transformers>=3.0.0
chromadb>=0.5.5

numpy>=1.26.4
requests>=2.31.0